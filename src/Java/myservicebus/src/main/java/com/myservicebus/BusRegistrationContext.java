package com.myservicebus;

/**
 * Marker interface representing a bus registration context. The
 * current implementation does not expose any members but acts as a
 * placeholder for future extensions and to keep the API aligned with
 * the .NET implementation where such a context is passed to endpoint
 * configuration methods.
 */
public interface BusRegistrationContext {
}

